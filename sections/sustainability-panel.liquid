<section class="hh-sus hh-section">
  <div class="hh-page hh-sus__grid">
    <div class="hh-sus__copy">
      <span class="hh-eyebrow">{{ section.settings.eyebrow | default: 'COMMITMENTS' }}</span>
      <h2 class="hh-h2">{{ section.settings.heading }}</h2>
      <p class="hh-lead">{{ section.settings.body }}</p>
      {%- if section.settings.cta_text != blank -%}<a class="hh-link" href="{{ section.settings.cta_link }}">{{ section.settings.cta_text }}</a>{%- endif -%}
    </div>
    <div class="hh-sus__stats">
      {%- for block in section.blocks -%}
        <div class="hh-sus__stat" {{ block.shopify_attributes }}>
          <strong class="hh-sus__number">{{ block.settings.number }}</strong>
          <span class="hh-sus__label">{{ block.settings.label }}</span>
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>
{% stylesheet %}
.hh-sus__grid { display: grid; grid-template-columns: 1fr 1fr; gap: 3rem; align-items: center; }
.hh-sus__copy { display: grid; gap: 1rem; max-width: 50ch; }
.hh-sus__stats { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
.hh-sus__stat { padding: 1.5rem; border: 1px solid rgba(0,0,0,0.1); border-radius: 8px; display: grid; gap: 0.5rem; }
.hh-sus__number { font-family: var(--hh-serif); font-size: clamp(2rem, 3vw, 2.75rem); line-height: 1; font-weight: 600; }
.hh-sus__label { font-size: 0.875rem; opacity: 0.7; line-height: 1.4; }
@media (max-width: 880px) { .hh-sus__grid { grid-template-columns: 1fr; } }
{% endstylesheet %}
{% schema %}
{ "name": "Sustainability panel", "tag": "section", "settings": [
  { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "COMMITMENTS" },
  { "type": "text", "id": "heading", "label": "Heading", "default": "What we measure." },
  { "type": "textarea", "id": "body", "label": "Body", "default": "Numbers, not claims. We publish an annual transparency report; here are the highlights." },
  { "type": "text", "id": "cta_text", "label": "CTA text", "default": "Read the full report" },
  { "type": "url", "id": "cta_link", "label": "CTA link" }
], "blocks": [
  { "type": "stat", "name": "Stat", "settings": [
    { "type": "text", "id": "number", "label": "Number", "default": "92%" },
    { "type": "text", "id": "label", "label": "Label", "default": "Of our packaging is post-consumer recycled" }
  ]}
], "max_blocks": 6, "presets": [{ "name": "Sustainability panel", "blocks": [
  { "type": "stat", "settings": { "number": "92%", "label": "Of our packaging is post-consumer recycled" } },
  { "type": "stat", "settings": { "number": "0", "label": "Animals tested on, ever" } },
  { "type": "stat", "settings": { "number": "1%", "label": "Of revenue donated to community partners" } },
  { "type": "stat", "settings": { "number": "30 days", "label": "No-questions return window" } }
]}]}
{% endschema %}
