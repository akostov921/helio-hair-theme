<section class="hh-cart hh-section" data-section-id="{{ section.id }}">
  <div class="hh-page hh-page--narrow">
    <header><span class="hh-eyebrow">Basket</span><h1 class="hh-h1">Your basket</h1></header>
    <div class="hh-rule"></div>
    {%- if cart.item_count > 0 -%}
      {%- form 'cart', cart -%}
        <ul class="hh-cart__items">
          {%- for item in cart.items -%}
            <li class="hh-cart__row" data-key="{{ item.key }}">
              <a href="{{ item.url }}" class="hh-cart__media">
                {%- if item.image -%}{{ item.image | image_url: width: 200 | image_tag: loading: 'lazy', alt: item.title | escape }}{%- else -%}<span>—</span>{%- endif -%}
              </a>
              <div class="hh-cart__meta">
                <a href="{{ item.url }}" class="hh-h3">{{ item.product.title }}</a>
                {%- if item.product.vendor != blank -%}<small class="hh-italic">{{ item.product.vendor }}</small>{%- endif -%}
                {%- if item.variant.title != 'Default Title' -%}<small>{{ item.variant.title }}</small>{%- endif -%}
              </div>
              <div class="hh-cart__qty">
                <input type="number" name="updates[{{ item.key }}]" value="{{ item.quantity }}" min="0" aria-label="Quantity">
                <a href="{{ routes.cart_change_url }}?id={{ item.key }}&quantity=0" class="hh-cart__remove">Remove</a>
              </div>
              <div class="hh-cart__price">{{ item.final_line_price | money_without_trailing_zeros }}</div>
            </li>
          {%- endfor -%}
        </ul>
        <footer class="hh-cart__foot">
          <div class="hh-cart__subtotal">
            <span class="hh-eyebrow">Subtotal</span>
            <strong class="hh-h2">{{ cart.total_price | money_with_currency }}</strong>
            <small>Tax and shipping calculated at checkout.</small>
          </div>
          <div class="hh-cart__actions">
            <button type="submit" name="update" class="hh-btn hh-btn--ghost">Update</button>
            <button type="submit" name="checkout" class="hh-btn">Checkout</button>
          </div>
        </footer>
      {%- endform -%}
    {%- else -%}
      <div class="hh-cart__empty">
        <p class="hh-display">An empty page.</p>
        <p class="hh-lead">It has its own beauty, but you probably wanted books.</p>
        <a class="hh-link" href="{{ routes.all_products_collection_url }}">Read the catalogue</a>
      </div>
    {%- endif -%}
  </div>
</section>

{% stylesheet %}
.hh-cart__items { list-style: none; padding: 0; margin: 0; }
.hh-cart__row {
  display: grid; grid-template-columns: 80px 1fr auto auto;
  gap: var(--hh-space-4);
  padding-block: var(--hh-space-4);
  border-bottom: 1px solid var(--hh-rule);
  align-items: center;
}
.hh-cart__media img { width: 100%; aspect-ratio: 3/4; object-fit: cover; background: var(--hh-paper-2); }
.hh-cart__media span { display: grid; place-items: center; aspect-ratio: 3/4; background: var(--hh-paper-2); font-family: var(--hh-serif); }
.hh-cart__meta { display: grid; gap: var(--hh-space-1); }
.hh-cart__meta a { color: inherit; }
.hh-cart__meta small { color: var(--hh-ink-muted); font-size: var(--hh-fs-sm); }
.hh-cart__qty input { width: 60px; padding: 0.3rem; border: 1px solid var(--hh-rule); font: inherit; text-align: center; }
.hh-cart__qty { display: grid; gap: var(--hh-space-1); justify-items: end; }
.hh-cart__remove { color: var(--hh-ink-muted); font-size: var(--hh-fs-xs); text-decoration: underline; }
.hh-cart__price { font-family: var(--hh-serif); font-size: 1.125rem; }
.hh-cart__foot { display: flex; justify-content: space-between; align-items: end; flex-wrap: wrap; gap: var(--hh-space-4); margin-top: var(--hh-space-5); }
.hh-cart__subtotal { display: grid; gap: var(--hh-space-1); }
.hh-cart__subtotal small { color: var(--hh-ink-muted); font-size: var(--hh-fs-sm); }
.hh-cart__actions { display: flex; gap: var(--hh-space-3); }
.hh-cart__empty { text-align: center; padding-block: var(--hh-space-9); display: grid; gap: var(--hh-space-4); justify-items: center; }
@media (max-width: 640px) { .hh-cart__row { grid-template-columns: 60px 1fr; row-gap: var(--hh-space-2); } .hh-cart__qty, .hh-cart__price { grid-column: 2; } }
{% endstylesheet %}

{% schema %}{ "name": "Cart", "tag": "section" }{% endschema %}
